// src/api/product/controllers/product.ts
// Strapi auto-generates this — no customization needed for basic CRUD.
// The default controller handles GET /api/products automatically.

// If you need custom logic, override it here:
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::product.product');
